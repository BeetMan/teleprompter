{
  "name": "teleprompter-workspace",
  "private": true,
  "version": "6.3.1",
  "description": "Development checks for the teleprompter desktop project.",
  "scripts": {
    "check": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/check-project.ps1",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
