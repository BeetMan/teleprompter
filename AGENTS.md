# Agent Instructions

## Agent skills

### Issue tracker

Issues and product requirements for this repository live in GitHub Issues. Use the `gh` CLI for issue operations. See `docs/agents/issue-tracker.md`.

### Domain docs

This is a single-context repository. Read `CONTEXT.md` and relevant records under `docs/adr/` when they exist. See `docs/agents/domain.md`.
